{
  "name": "be--ppm-platform",
  "version": "0.0.1",
  "description": "Api rest for Hackthon Tech Summit CaixaBank Tech",
  "main": "dist/Application.js",
  "scripts": {
    "dev": "npm run clean && tsc & cross-env ENV=dev node dist/Application.js",
    "pro": "npm run clean && tsc & cross-env ENV=pro node dist/Application.js",
    "clean": "rimraf dist",
    "build": "npm run clean && tsc"
  },
  "author": "Techtastic",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.18",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "express": "^4.18.2",
    "rimraf": "^5.0.5"
  }
}